import React, { useState } from "react";

const customer = [
   { id: 1,  name: 'costomer_1', message: "The traditional way of calling attention to a certain web page elements has beenmethod of phem in a sticky" },
   { id:2 ,  name: 'costomer_2', message: " The app is at danger of starting to resemble a control panel from a jumbo jet cockpit." },
   { id: 3,  name: 'costomer_3', message: "There should be a better way of managing a user attention and React Reveal can do just that." },
   { id: 4,  name: 'costomer_4', message: "Instead of trying to squeeze everything that requires attention into one screenant bits as they scroll past." },
   { id: 5,  name: 'costomer_5', message: "The other problem many single page applications are facing is actually their speed." },
   { id: 6,  name: 'costomer_6', message: "React Reveal is MIT licensed, supports server side rendering, ess your SEO, compatible with react transition" },
]   

export  const  TextSlider=()=>{

    // const [wordData ,setWordData] = useState(customer[0]);
    // const handleClick =  (index)=>{
    //     const wordSlider = customer[index]
    //     setWordData(wordSlider)
    // }

    // return     customer.map((custem,index)=><div key={index} className="about_us" style={{marginTop:'2rem' ,marginBottom:'2rem'}}>
    //        <span style={{fontSize:'23px'}}>{custem.name}</span> 
    //         <h4>Our Customer</h4>
    //         <p>{custem.message}</p>
    //         <button className={wordData.id === index ? "clicked" : ""} onClick={()=>handleClick(index)}>123</button>
    //     </div>)
        
}